[package]
name = "plugin_api"
version = "0.1.0"
authors = ["HexHawk Team <dev@hexhawk.local>"]
edition = "2021"

[lib]
name = "plugin_api"
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[package.metadata]
license = "MIT OR Apache-2.0"
