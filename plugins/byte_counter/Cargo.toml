[package]
name = "byte_counter"
version = "0.1.0"
authors = ["HexHawk Team <dev@hexhawk.local>"]
edition = "2021"

[lib]
name = "byte_counter"
crate-type = ["cdylib"]

[dependencies]
plugin_api = { path = "../../plugin-api" }
serde_json = "1.0"
